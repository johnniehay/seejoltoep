Index: src/lib/payload-authjs-custom/types/payload-types.ts
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/src/lib/payload-authjs-custom/types/payload-types.ts b/src/lib/payload-authjs-custom/types/payload-types.ts
--- a/src/lib/payload-authjs-custom/types/payload-types.ts
+++ b/src/lib/payload-authjs-custom/types/payload-types.ts
@@ -167,6 +167,7 @@
     | {
         token: string;
         expires: string;
+        uses_remaining: number;
         id?: string | null;
       }[]
     | null;
@@ -221,6 +222,7 @@
     | {
         token: string;
         expires: string;
+        uses_remaining: number;
         id?: string | null;
       }[]
     | null;
@@ -351,6 +353,7 @@
     | {
         token?: T;
         expires?: T;
+        uses_remaining?: T;
         id?: T;
       };
   authenticators?:
@@ -404,6 +407,7 @@
     | {
         token?: T;
         expires?: T;
+        uses_remaining?: T;
         id?: T;
       };
   authenticators?:
Index: src/lib/payload-authjs-custom/authjs/utils/transform.ts
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/src/lib/payload-authjs-custom/authjs/utils/transform.ts b/src/lib/payload-authjs-custom/authjs/utils/transform.ts
--- a/src/lib/payload-authjs-custom/authjs/utils/transform.ts
+++ b/src/lib/payload-authjs-custom/authjs/utils/transform.ts
@@ -120,8 +120,11 @@
      * Modifications:
      * - ~`identifier`~
      */
-    fromAdapter: (adapterVerificationToken: AdapterVerificationToken): VerificationToken => {
-      return transformAdapterObjectToPayloadObject(omit(adapterVerificationToken, ["identifier"]));
+    fromAdapter: (adapterVerificationToken: AdapterVerificationToken, uses_remaining:number): VerificationToken => {
+      return {
+        uses_remaining,
+        ...transformAdapterObjectToPayloadObject(omit(adapterVerificationToken, ["identifier"]))
+      }
     },
   },
   authenticator: {
Index: src/lib/payload-authjs-custom/authjs/PayloadAdapter.ts
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/src/lib/payload-authjs-custom/authjs/PayloadAdapter.ts b/src/lib/payload-authjs-custom/authjs/PayloadAdapter.ts
--- a/src/lib/payload-authjs-custom/authjs/PayloadAdapter.ts
+++ b/src/lib/payload-authjs-custom/authjs/PayloadAdapter.ts
@@ -565,7 +565,7 @@
         const newUser = {
           id: crypto.randomUUID(),
           email,
-          verificationTokens: [transform.verificationToken.fromAdapter(adapterVerificationToken)],
+          verificationTokens: [transform.verificationToken.fromAdapter(adapterVerificationToken, 5)], //TODO: Remove hardcoded uses_remaining make configurable
         };

         if (!(await payload).collections[userCollectionSlug]?.config.auth.disableLocalStrategy) {
@@ -596,7 +596,7 @@
           data: {
             verificationTokens: [
               ...(payloadUser.verificationTokens || []),
-              transform.verificationToken.fromAdapter(adapterVerificationToken),
+              transform.verificationToken.fromAdapter(adapterVerificationToken, 5), //TODO: Remove hardcoded uses_remaining make configurable
             ],
           },
           select: {
@@ -606,6 +606,10 @@
           },
         });
       }
+      /*
+                    ...(payloadUser.verificationTokens?.filter(t => t.uses_remaining > 0 )
+                .filter(t => Date.parse(t.expires) > Date.now()) || []),
+       */

       const createdToken = payloadUser.verificationTokens?.find(
         t => t.token === adapterVerificationToken.token,
@@ -643,6 +647,17 @@
       if (!payloadUser) {
         return null;
       }
+      // Purge all overused or expired tokens
+      payloadUser = await (
+        await payload
+      ).update({
+        collection: userCollectionSlug,
+        id: payloadUser.id,
+        data: {
+          verificationTokens: payloadUser.verificationTokens?.filter(t => t.uses_remaining > 0 )
+            .filter(t => Date.parse(t.expires) > Date.now()),
+        },
+      });

       const verificationToken = payloadUser.verificationTokens?.find(t => t.token === token);
       if (!verificationToken) {
@@ -655,7 +670,7 @@
         collection: userCollectionSlug,
         id: payloadUser.id,
         data: {
-          verificationTokens: payloadUser.verificationTokens?.filter(t => t.token !== token),
+          verificationTokens: payloadUser.verificationTokens?.filter(t => t.token !== token || t.uses_remaining > 1 ).map(t => t.token === token ? {...t, uses_remaining: t.uses_remaining-1 } : t),
         },
         select: {
           id: true,
Index: src/lib/payload-authjs-custom/payload/collection/fields/verificationTokens.ts
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/src/lib/payload-authjs-custom/payload/collection/fields/verificationTokens.ts b/src/lib/payload-authjs-custom/payload/collection/fields/verificationTokens.ts
--- a/src/lib/payload-authjs-custom/payload/collection/fields/verificationTokens.ts
+++ b/src/lib/payload-authjs-custom/payload/collection/fields/verificationTokens.ts
@@ -14,6 +14,7 @@
       fields: [
         { name: "token", type: "text", required: true, index: true },
         { name: "expires", type: "date", required: true },
+        { name: "uses_remaining", type: "number",required: true,defaultValue:5}
       ],
     },
   ],
Index: src/lib/payload-authjs-custom/tsconfig.json
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/src/lib/payload-authjs-custom/tsconfig.json b/src/lib/payload-authjs-custom/tsconfig.json
new file mode 100644
--- /dev/null
+++ b/src/lib/payload-authjs-custom/tsconfig.json
@@ -0,0 +1,52 @@
+{
+  "compilerOptions": {
+    "strict": true,
+    "baseUrl": ".",
+    "esModuleInterop": true,
+    "target": "ES2022",
+    "lib": [
+      "DOM",
+      "DOM.Iterable",
+      "ES2022",
+    ],
+    "allowJs": true,
+    "skipLibCheck": true,
+    "noEmit": true,
+    "incremental": true,
+    "jsx": "react-jsx",
+    "module": "esnext",
+    "moduleResolution": "bundler",
+    "resolveJsonModule": true,
+    "sourceMap": true,
+    "isolatedModules": true,
+    "plugins": [
+      {
+        "name": "next"
+      }
+    ],
+    "paths": {
+      "@payload-config": [
+        "./src/payload.config.ts"
+      ],
+      "react": [
+        "./node_modules/@types/react"
+      ],
+      "@/*": [
+        "./src/*"
+      ]
+    }
+  },
+  "include": [
+    "**/*.ts",
+    "**/*.tsx",
+    ".next/types/**/*.ts",
+    "redirects.js",
+    "next-env.d.ts",
+    "next.config.js",
+    "next-sitemap.config.cjs",
+    ".next/dev/types/**/*.ts"
+  ],
+  "exclude": [
+    "node_modules"
+  ]
+}
